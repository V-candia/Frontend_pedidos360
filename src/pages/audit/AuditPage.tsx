export function AuditPage() {
  return (
    <div>
      <h1>Auditoría</h1>
      <p>Pendiente de conectar con ms-pedidos360-audit (eventos Kafka) vía API Gateway.</p>
      <p style={{ color: "#888" }}>Sin eventos — backend no disponible.</p>
    </div>
  );
}
