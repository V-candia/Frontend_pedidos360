import { PlaceholderPage } from "../../components/PlaceholderPage";

export function ReportsPage() {
  return (
    <PlaceholderPage
      title="Reportería"
      pending="Pendiente de conectar con ms-pedidos360-report (streaming Kafka) vía API Gateway."
      empty="Sin datos de ventas por hora, lead time ni estados activos."
    />
  );
}
